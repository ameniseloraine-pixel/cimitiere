# ════════════════════════════════════════════
# INSTALLATION MINIMALE — Windows / démarrage rapide
# Python 3.10+ requis
# ════════════════════════════════════════════
# pip install -r requirements.txt

# Framework
django==4.2.11
django-ninja==1.1.0

# Authentification JWT
PyJWT==2.8.0
django-cors-headers==4.3.1

# Audit trail
django-simple-history==3.5.0

# Tâches planifiées (BDD uniquement, sans Redis)
django-celery-beat==2.6.0
celery==5.3.6

# Utilitaires
python-decouple==3.8
python-dateutil==2.9.0.post0
django-filter==23.5

# Génération PDF (factures, contrats, PV exhumation)
reportlab==4.1.0

# Exports Excel
openpyxl==3.1.2

# Fichiers statiques
whitenoise==6.6.0

# Serveur WSGI
gunicorn==21.2.0

# ════════════════════════════════════════════
# OPTIONNEL — activer selon les besoins
# ════════════════════════════════════════════

# Email avancé (Mailgun, SendGrid, etc.)
# django-anymail==10.3

# Stockage fichiers distant (S3, etc.)
# django-storages==1.14.2
# Pillow==10.2.0

# ════════════════════════════════════════════
# PostgreSQL + PostGIS (requis)
# ════════════════════════════════════════════
psycopg2-binary==2.9.9

# Parsing de DATABASE_URL fourni automatiquement par Render en production
dj-database-url==2.1.0

# Redis pour Celery (alertes asynchrones)
# redis==5.0.3

# WeasyPrint (alternative à ReportLab, nécessite GTK sous Windows)
# weasyprint==62.3